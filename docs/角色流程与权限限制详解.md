# DecoFinance 蝟餌?閫瘚?銝????嗉祕閫?
**?湔?園**: 2026-03-24  
**?**: v2.0 (??唳????

---

## 銝???脣?銋??詨??提

### 1. customer嚗恥??銝蜓嚗?**?提**: 鋆耨憿寧?絲?對?韐提??憿寧??踹??恣?寥?蝔???甈?
**?詨???**:
- ??憿寧蝞∠?嚗?撱箝?颲??方撌梁?憿寧嚗?- ????蝞∠?嚗??????嚗?- ????蝣恣??- ???絲鈭悅
- ??銝蝞∠??砍
- ??銝憭?韐瑟狡摰⊥

---

### 2. company_user嚗?貊???踹???
**?提**: 鋆耨?砍隞?”嚗?韐???銵★?柴霂瑁晰甈?
**?詨???**:
- ???砍蝞∠?嚗?撱?*1 銝?*?砍??颲撌梁??砍嚗?- ??憿寧??
- ??韐瑟狡?唾窈??甈?- ???漱憿寧??蝣?- ??銝摰⊥韐瑟狡
- ??銝閫?鈭悅
- ??銝??唳

**?寞??**:
- ?? **銝撖嫣??**: 瘥葵?砍?冽?芾?遣???銝芸??- ?? **?唳?氖**: ?芾?亦??芸楛?砍???
---

### 3. reviewer嚗恣?詨?嚗?**?提**: 撟喳摰⊥鈭箏?嚗?韐?晰甈曉恣?嫘?霈株圾??
**?詨???**:
- ??韐瑟狡摰⊥嚗????嚗?- ??韐瑟狡?
- ??鈭悅閫?
- ???亦????殷??砍?晰甈整★?柴?霈殷?
- ??摰∟恣?亙??亦?
- ??銝??砍/韐瑟狡
- ??銝?遣憿寧???
---

### 4. admin嚗恣??嚗?**?提**: 蝟餌?蝞∠????交??擃???
**?詨???**:
- ??????脩??券??
- ????砍?晰甈整★??- ???冽蝞∠?
- ??蝟餌??蔭

---

## 鈭祕蝏??⊥?蝔????批

### 2.1 ?砍蝞∠?瘚?

#### 瘚? 1: 瘛餃??砍

```
頝臬?嚗ET/POST /companies/add
??嚗dmin, reviewer, company_user
```

**瘚?甇仿炊**:

1. **霈輸??璉??*
   - `@role_required('admin', 'reviewer', 'company_user')`
   - 敹◆?餃?銝蛹??閫

2. **?砍?冽?寞??**
   ```python
   if g.user.role == 'company_user' and not company_user_can_add(g.user):
       flash('?典歇蝏??銝銝芸?賂?銝?溶???詻?, 'warning')
       return redirect(url_for('companies.list_companies'))
   ```
   - 璉?交隞塚?`user.company_id is None`
   - 憒?撌脣??賂?????”憿?
3. **?遣?砍**
   - 憛怠??砍靽⊥銵典?
   - ?芸霈∠???韏 (`_derive_bid_eligibility`)
   - 靽??唳?桀?

4. **?唾??冽**
   ```python
   if g.user.role == 'company_user' and g.user.company_id is None:
       g.user.company_id = company.id
   ```
   - ?砍?冽?遣?砍??典??   - 蝞∠???摰⊥??隡?典??
**???拚**:

| 閫 | ?航挪??| ?圈?? | ?芸?唾? |
|------|--------|----------|----------|
| admin | ??| ????| ??|
| reviewer | ??| ????| ??|
| company_user | ??| 隞?1 銝?| ??|
| customer | ??| - | - |

---

#### 瘚? 2: ?亦??砍?”

```
頝臬?嚗ET /companies/
??嚗ogin_required嚗??敶?瘀?
```

**?唳霈輸?**:

```python
# ?寞閫餈誘?唳霈輸?
if g.user.role == 'company_user':
    # ?砍?冽?芾霈輸?芸楛???    query = query.filter_by(id=g.user.company_id)
```

**???拚**:

| 閫 | 霈輸? | 霂湔? |
|------|----------|------|
| customer | ??| 璅⊥銝剝???"Add Company"?嚗? g.user 銝? admin/reviewer嚗?|
| company_user | ??隞撌勗??| `filter_by(id=g.user.company_id)` |
| reviewer | ??????| ??皛?|
| admin | ??????| ??皛?|

**?垢?批** (templates/companies/list.html):
```html
{% if g.user and (g.user.role == 'admin' or g.user.role == 'reviewer' or (g.user.role == 'company_user' and not g.user.company_id)) %}
<!-- ?曄內"Add Company"???蝝Ｘ? -->
{% endif %}
```

---

#### 瘚? 3: ?亦??砍霂行?

```
頝臬?嚗ET /companies/<id>
??嚗ogin_required
```

**霈輸?批**:
- ??敶?琿?航挪??- 雿?貊?瑕?賜??啗撌勗?賂??”撌脰?皛歹?

**撅內?捆**:
- ?砍?箸靽⊥
- 靽∠霂??
- 韐瑟狡?唾窈?”
- ???扯郎??
---

#### 瘚? 4: 蝻??砍

```
頝臬?嚗ET/POST /companies/<id>/edit
??嚗login_required + require_ownership(can_manage_company(g.user, company))
```

**??璉?仿餉?**:

```python
def can_manage_company(user, company):
    if user is None:
        return False
    if user.role == 'admin':
        return True
    if user.role == 'reviewer':
        return True
    return user.company_id == company.id  # ?砍?冽?芾蝻??芸楛???```

**???拚**:

| 閫 | ?舐?颲?| ?∩辣 |
|------|--------|------|
| admin | ??| ????|
| reviewer | ??| ????|
| company_user | ??| `user.company_id == company.id` |
| customer | ??| - |

---

#### 瘚? 5: 霈∠?靽∠霂?

```
頝臬?嚗OST /companies/<id>/score
??嚗login_required
```

**霈輸?批**:
- ??敶?琿?航圻??- 雿? `can_manage_company` ???批嚗芋?蹂葉??曄內?批嚗?
**?垢?批**:
```html
<!-- 隞?豢??恣???恣?詨??航? -->
{% if g.user and (g.user.role == 'admin' or g.user.role == 'reviewer' or can_manage_company(g.user, company)) %}
<button>Calculate Score</button>
{% endif %}
```

---

#### 瘚? 6: ??砍

```
頝臬?嚗OST /companies/<id>/delete
??嚗role_required('admin')
```

**??**: 隞恣??

---

#### 瘚? 7: 撖寞?靽∠?亙?

```
頝臬?嚗ET /companies/compare-report
??嚗role_required('admin', 'reviewer')
```

**??**: 隞恣???恣?詨?

**?**: ??憭?3 摰嗅?貉?銵笆瘥???
---

### 2.2 韐瑟狡蝞∠?瘚?

#### 瘚? 1: ?亦?韐瑟狡?”

```
頝臬?嚗ET /loans/
??嚗role_required('company_user', 'admin', 'reviewer')
```

**?唳餈誘**:

```python
if g.user.role == 'company_user':
    # ?砍?冽?芾?亦??芸楛?砍?晰甈?    query = query.filter_by(company_id=g.user.company_id)
```

**???拚**:

| 閫 | 霈輸? | 霂湔? |
|------|----------|------|
| customer | ??| ???挪??|
| company_user | ??隞撌勗??| ?唳?氖 |
| reviewer | ????晰甈?| ?典?閫 |
| admin | ????晰甈?| ?典?閫 |

---

#### 瘚? 2: ?唾窈韐瑟狡

```
頝臬?嚗ET/POST /loans/add
??嚗role_required('company_user')
```

**???**:
- ??隞?貊?瑕?漱?唾窈
- ??蝞∠????臭誑嚗??虜銝?乩蝙?剁?
- ??摰⊥???賣?鈭歹??踹??拍??脩?嚗?- ??摰Ｘ銝?漱

**撉??餉?**:

```python
if g.user.role == 'company_user':
    require_ownership(g.user.company_id == company_id)
```

**?舫?★??*:
```python
if project_id is not None and g.user.role == 'company_user':
    require_ownership(
        project is not None
        and project.accepted_bid_id is not None
        and any(bid.id == project.accepted_bid_id and bid.company_id == g.user.company_id for bid in project.bids)
    )
```
- ?芾?唾??芸楛?砍銝剜??★??
---

#### 瘚? 3: ?亦?韐瑟狡霂行?

```
頝臬?嚗ET /loans/<id>
??嚗role_required('company_user', 'admin', 'reviewer')
```

**霈輸?批?賣**:

```python
def _can_access_loan(application):
    if g.user.role in ['admin', 'reviewer']:
        return True
    if g.user.role == 'company_user':
        return g.user.company_id == application.company_id
    return False
```

**???拚**:

| 閫 | ?航挪??| ?∩辣 |
|------|--------|------|
| customer | ??| - |
| company_user | ??| ?芸楛?砍?晰甈?|
| reviewer | ??| ??晰甈?|
| admin | ??| ??晰甈?|

---

#### 瘚? 4: 摰⊥韐瑟狡

```
頝臬?嚗ET/POST /loans/<id>/review
??嚗role_required('reviewer', 'admin')
```

**???★**:

1. **?孵?韐瑟狡**
   ```python
   application.application_status = 'approved'
   application.approved_amount = float(request.form.get('approved_amount'))
   application.approved_interest_rate = float(request.form.get('approved_interest_rate'))
   ```

2. **??韐瑟狡**
   ```python
   application.application_status = 'rejected'
   application.rejection_reason = request.form.get('rejection_reason')
   ```

**??**: 隞恣?詨??恣??

---

#### 瘚? 5: ?韐瑟狡

```
頝臬?嚗OST /loans/<id>/disburse
??嚗role_required('reviewer', 'admin')
```

**?蔭?∩辣**:
```python
if application.application_status != 'approved':
    flash('Application must be approved before disbursement.', 'error')
```

**??**:
- 霈曄蔭??交?
- 霈啣????
- ?湔餈狡?嗆蛹"current"

---

#### 瘚? 6: 餈狡

```
頝臬?嚗OST /loans/<id>/repay
??嚗role_required('company_user', 'admin', 'reviewer')
```

**霈輸?批**:
```python
require_ownership(_can_access_loan(application))
```

**???拚**:

| 閫 | ?航?甈?| ?∩辣 |
|------|--------|------|
| customer | ??| - |
| company_user | ??| ?芸楛?砍?晰甈?|
| reviewer | ??| ??晰甈?|
| admin | ??| ??晰甈?|

---

#### 瘚? 7: ?韐瑟狡

```
頝臬?嚗OST /loans/<id>/delete
??嚗role_required('admin')
```

**??**: 隞恣??

---

### 2.3 憿寧蝞∠?瘚?

#### 瘚? 1: ?亦?憿寧?”

```
頝臬?嚗ET /projects/
??嚗login_required
```

**???脰?皛?*:

```python
if g.user.role == 'customer':
    # 摰Ｘ?芾??芸楛?★??    projects = Project.query.filter_by(customer_user_id=g.user.id).all()
elif g.user.role == 'company_user':
    # ?砍?冽嚗??暹??★??+ ???★??    company = _current_user_company()
    projects = [
        project for project in visible_projects
        if project.status == 'open_for_bids' or _company_has_project_access(project, company.id)
    ]
else:
    # 蝞∠???摰⊥????★??    projects = Project.query.all()
```

**???拚**:

| 閫 | 霈輸? |
|------|----------|
| customer | ??隞撌勗?撱箇?憿寧 |
| company_user | ??撘?暹??★??+ ???★??|
| reviewer | ????★??|
| admin | ????★??|

---

#### 瘚? 2: ?遣憿寧

```
頝臬?嚗ET/POST /projects/add
??嚗role_required('customer', 'admin')
```

**??**:
- ??摰Ｘ嚗?撱箄撌梁?鋆耨憿寧
- ??蝞∠???隞?恥?瑕?撱?- ???砍?冽嚗??賢?撱綽??踹?雿?嚗?- ??摰⊥??銝?遣

---

#### 瘚? 3: 蝻?憿寧

```
頝臬?嚗ET/POST /projects/<id>/edit
??嚗role_required('customer', 'admin')
```

**??璉??*:
```python
require_ownership(project.customer_user_id == g.user.id or g.user.role == 'admin')
```

**?嗆???*:
```python
if g.user.role != 'admin' and project.status not in ['draft', 'open_for_bids']:
    flash('Only draft or open-for-bids projects can be edited.', 'warning')
```
- ?芣?"?阮"??撘?暹????嗆?憿寧?舐?颲?- 撌脫????歇蝑曄漲?★?桐??舐?颲?
---

#### 瘚? 4: ?亦?憿寧霂行?

```
頝臬?嚗ET /projects/<id>
??嚗login_required + _require_project_access(project)
```

**霈輸?批?餉?**:

```python
def _require_project_access(project):
    if g.user.role in ['admin', 'reviewer']:
        return  # ????    if g.user.role == 'customer':
        require_ownership(project.customer_user_id == g.user.id)
        return
    if g.user.role == 'company_user':
        company = _current_user_company()
        require_ownership(
            project.status == 'open_for_bids' or 
            _company_has_project_access(project, company.id)
        )
        return
    require_ownership(False)
```

**?砍霈輸憿寧?斗**:

```python
def _company_has_project_access(project, company_id):
    if not company_id:
        return False
    # 璉?亙?豢?行?鈭支????葉??    if any(bid.company_id == company_id for bid in project.bids):
        return True
    return bool(
        project.accepted_bid_id and 
        any(bid.id == project.accepted_bid_id and bid.company_id == company_id for bid in project.bids)
    )
```

**???拚**:

| 閫 | ?航挪??| ?∩辣 |
|------|--------|------|
| customer | ??| ?芸楛?遣?★??|
| company_user | ??| 撘?暹??★??OR 撌脫?鈭斗???OR 銝剜?憿寧 |
| reviewer | ??| ??★??|
| admin | ??| ??★??|

---

#### 瘚? 5: ?漱??

```
頝臬?嚗OST /projects/<id>/bids
??嚗role_required('company_user', 'admin')
```

**?蔭?∩辣璉??*:

1. **憿寧?嗆???*
   ```python
   if project.status != 'open_for_bids':
       flash('This project is not accepting bids.', 'warning')
   ```

2. **?砍韏璉??*
   ```python
   company = db.session.get(Company, g.user.company_id)
   if not company_can_bid(company):
       flash('Your company is not verified for bidding.', 'danger')
   ```

3. **????璉??*
   ```python
   existing_bid = ProjectBid.query.filter(
       ProjectBid.project_id == project.id,
       ProjectBid.company_id == company.id,
       ProjectBid.status.in_(['submitted', 'shortlisted', 'accepted'])
   ).first()
   if existing_bid:
       flash('Your company already has an active bid on this project.', 'warning')
   ```

**???拚**:

| 閫 | ?舀???| ?∩辣 |
|------|--------|------|
| customer | ??| - |
| company_user | ??| ?砍撉??? + 憿寧撘?暹???+ ?暑頝???|
| reviewer | ??| - |
| admin | ??| - |

---

#### 瘚? 6: ?亙???

```
頝臬?嚗OST /projects/<id>/bids/<bid_id>/accept
??嚗role_required('customer', 'admin')
```

**??璉??*:
```python
require_ownership(project.customer_user_id == g.user.id or g.user.role == 'admin')
```

**?蔭?∩辣**:
1. 憿寧?嗆?憿餅 `open_for_bids`
2. 撠?亙??嗡???

**??**:
- ?湔憿寧?嗆蛹 `contracted`
- ?湔???嗆蛹 `accepted`
- ?遣?箄?漲
- 霈啣?摰∟恣?亙?

---

#### 瘚? 7: 瘛餃???蝣?
```
頝臬?嚗OST /projects/<id>/milestones/add
??嚗role_required('customer', 'admin')
```

**?蔭?∩辣**:
```python
if project.status not in ['contracted', 'in_progress']:
    flash('Milestones can only be added after a bid has been accepted.', 'warning')
```

**??**:
- ??摰Ｘ嚗溶?撌梢★?桃???蝣?- ??蝞∠???隞?恥?瑟溶??- ???砍?冽嚗??賣溶??雿?漱嚗?
---

#### 瘚? 8: ?漱??蝣?
```
頝臬?嚗OST /projects/milestones/<id>/submit
??嚗role_required('company_user', 'admin')
```

**??璉??*:
```python
if g.user.role == 'company_user':
    require_ownership(
        company is not None and 
        accepted_bid is not None and 
        accepted_bid.company_id == company.id
    )
```

**?蔭?∩辣**:
1. 憿寧?嗆?憿餅 `contracted` ??`in_progress`
2. ??蝣??憿餅 `planned`
3. ?砍敹◆?臭葉???
**??**:
- ?湔??蝣?蛹 `submitted`
- 霈啣??漱?園??鈭支犖
- ?湔憿寧?嗆蛹 `in_progress`嚗?? contracted嚗?
---

#### 瘚? 9: ?孵???蝣?
```
頝臬?嚗OST /projects/milestones/<id>/approve
??嚗role_required('customer', 'admin')
```

**??璉??*:
```python
require_ownership(project.customer_user_id == g.user.id or g.user.role == 'admin')
```

**?蔭?∩辣**:
1. ??蝣??憿餅 `submitted`
2. ?閫???霈?
**璉?乩?霈?*:
```python
def _milestone_has_open_disputes(milestone):
    return any(
        dispute.status == 'open' and dispute.milestone_id in [None, milestone.id]
        for dispute in milestone.project.disputes
    )
```

**??**:
- ?湔??蝣?蛹 `approved`
- ??恣韏?
- ?湔憿寧?嗆?憒????蝔??賢歇摰?嚗挽蝵桐蛹 `completed`嚗?
---

### 2.4 鈭悅蝞∠?瘚?

#### 瘚? 1: ?亦?鈭悅?”

```
頝臬?嚗ET /disputes/
??嚗login_required
```

**???脰?皛?*:

```python
if g.user.role == 'customer':
    # 摰Ｘ嚗撌梢★?桃?鈭悅
    disputes = [item for item in disputes if item.project.customer_user_id == g.user.id]
elif g.user.role == 'company_user':
    # ?砍?冽嚗??撌勗?貊?鈭悅
    company_id = _current_user_company_id()
    disputes = [
        item for item in disputes
        if item.against_company_id == company_id or item.opened_by_user_id == g.user.id
    ]
```

**???拚**:

| 閫 | 霈輸? |
|------|----------|
| customer | ???芸楛憿寧??霈?|
| company_user | ??瘨??芸楛?砍??霈?OR ?芸楛?絲??霈?|
| reviewer | ?????霈?|
| admin | ?????霈?|

---

#### 瘚? 2: ?絲鈭悅

```
頝臬?嚗OST /disputes/add
??嚗role_required('customer', 'company_user', 'admin')
```

**??璉??*:

```python
if g.user.role == 'customer':
    require_ownership(project.customer_user_id == g.user.id)
elif g.user.role == 'company_user':
    company_id = _current_user_company_id()
    require_ownership(_company_participates_in_project(project, company_id))
```

**?砍??憿寧?斗**:
```python
def _company_participates_in_project(project, company_id):
    if not company_id:
        return False
    return any(bid.company_id == company_id for bid in project.bids)
```

**???拚**:

| 閫 | ?臬?韏?| ?∩辣 |
|------|--------|------|
| customer | ??| 敹◆?舫★?桐?銝?|
| company_user | ??| 敹◆??憿寧嚗?鈭斗???銝剜?嚗?|
| reviewer | ??| - |
| admin | ??| - |

---

#### 瘚? 3: 閫?鈭悅

```
頝臬?嚗OST /disputes/<id>/resolve
??嚗role_required('reviewer', 'admin')
```

**??**: 隞恣?詨??恣??

**??**:
- ?湔鈭悅?嗆蛹 `resolved`
- 霈啣?閫??寞?
- 閫??恣韏?
- 霈啣?摰∟恣?亙?

---

### 2.5 蝟餌?蝞∠?瘚?

#### 瘚? 1: ?亦?摰∟恣?亙?

```
頝臬?嚗ET /admin/audit-logs
??嚗role_required('admin', 'reviewer')
```

**??**: 隞恣???恣?詨?

**?**: 隞蝷箸?餈?100 ?∟扇敶?
---

#### 瘚? 2: ?冽瘜典?

```
頝臬?嚗ET/POST /auth/register
??嚗撘
```

**?航瘜典?閫**:
```python
SELF_REGISTRATION_ROLES = {'customer', 'company_user'}
```

**?**:
- ??customer: ?航瘜典?
- ??company_user: ?航瘜典?
- ??reviewer: ?閬??啣?撱?- ??admin: ?閬??啣?撱?
---

## 銝???嗅極?瑕??
### 3.1 鋆弘??
#### `@login_required`
```python
def login_required(f):
    @wraps(f)
    def decorated_function(*args, **kwargs):
        if 'user_id' not in session:
            flash('Please log in to access this page.', 'warning')
            return redirect(url_for('auth.login'))
        if getattr(g, 'user', None) is not None and not g.user.is_active:
            session.pop('user_id', None)
            flash('Your account is inactive.', 'danger')
            return redirect(url_for('auth.login'))
        return f(*args, **kwargs)
    return decorated_function
```

**?**:
1. 璉?亦?瑟?衣敶?2. 璉?亥揭?瑟?行?瘣?3. ?芰敶?摰??啁敶△

---

#### `@role_required(*roles)`
```python
def role_required(*roles):
    def decorator(f):
        @wraps(f)
        @login_required
        def decorated_function(*args, **kwargs):
            if g.user is None or g.user.role not in roles:
                flash('You do not have permission to access this page.', 'danger')
                return redirect(url_for('main.index'))
            return f(*args, **kwargs)
        return decorated_function
    return decorator
```

**?**:
1. ?蔭 `@login_required`
2. 璉?亦?瑁??脫?血?捂???脣?銵其葉
3. ????摰??圈?憿?
---

### 3.2 銝???賣

#### `can_manage_company(user, company)`
```python
def can_manage_company(user, company):
    if user is None:
        return False
    if user.role == 'admin':
        return True
    if user.role == 'reviewer':
        return True
    return user.company_id == company.id
```

**?券?*: ?砍蝞∠???璉?伐?蝻?????嚗?
---

#### `company_user_can_add(user)`
```python
def company_user_can_add(user):
    """Check if a company_user can add a new company (only one allowed)"""
    if user is None or user.role != 'company_user':
        return True
    return user.company_id is None
```

**?券?*: 璉?亙?貊?瑟?血瘛餃??啣?賂?銝撖嫣??嚗?
---

#### `require_ownership(condition)`
```python
def require_ownership(condition):
    if not condition:
        abort(403)
```

**?券?*: 撘箏???璉?伐?憭梯揖????403 ?秤

---

#### `user_has_company(user)`
```python
def user_has_company(user):
    """Check if a user already has an associated company"""
    if user is None:
        return False
    return user.company_id is not None
```

**?券?*: 璉?亦?瑟?血歇?唾??砍

---

### 3.3 ?孵?銝霈輸?批

#### 韐瑟狡霈輸?批 `_can_access_loan(application)`
```python
def _can_access_loan(application):
    if g.user.role in ['admin', 'reviewer']:
        return True
    if g.user.role == 'company_user':
        return g.user.company_id == application.company_id
    return False
```

---

#### 憿寧霈輸?批 `_require_project_access(project)`
```python
def _require_project_access(project):
    if g.user.role in ['admin', 'reviewer']:
        return
    if g.user.role == 'customer':
        require_ownership(project.customer_user_id == g.user.id)
        return
    if g.user.role == 'company_user':
        company = _current_user_company()
        require_ownership(
            project.status == 'open_for_bids' or 
            _company_has_project_access(project, company.id if company else None)
        )
        return
    require_ownership(False)
```

---

#### ?砍??韏璉??`_company_has_project_access(project, company_id)`
```python
def _company_has_project_access(project, company_id):
    if not company_id:
        return False
    # 璉?亙?豢?行?鈭支????葉??    if any(bid.company_id == company_id for bid in project.bids):
        return True
    return bool(
        project.accepted_bid_id and 
        any(bid.id == project.accepted_bid_id and bid.company_id == company_id for bid in project.bids)
    )
```

---

## ??格?桅?蝳餌???
### 4.1 ?砍?唳?氖

| 閫 | ?”霈輸 | 霂行?霈輸 | 蝻??? |
|------|----------|----------|----------|
| customer | ??| ??| ??|
| company_user | ??隞撌?| ??隞撌?| ??隞撌?|
| reviewer | ?????| ?????| ?????|
| admin | ?????| ?????| ?????|

**摰?孵?**:
```python
# ?”餈誘
if g.user.role == 'company_user':
    query = query.filter_by(id=g.user.company_id)

# 蝻???
require_ownership(can_manage_company(g.user, company))
```

---

### 4.2 韐瑟狡?唳?氖

| 閫 | ?”霈輸 | 霂行?霈輸 | ?唾窈?? | 摰⊥?? |
|------|----------|----------|----------|----------|
| customer | ??| ??| ??| ??|
| company_user | ??隞撌?| ??隞撌?| ??隞撌?| ??|
| reviewer | ?????| ?????| ??| ??|
| admin | ?????| ?????| ??| ??|

**摰?孵?**:
```python
# ?”餈誘
if g.user.role == 'company_user':
    query = query.filter_by(company_id=g.user.company_id)

# 霂行?霈輸
require_ownership(_can_access_loan(application))
```

---

### 4.3 憿寧?唳?氖

| 閫 | ?”霈輸 | 霂行?霈輸 | ?遣?? | ???? |
|------|----------|----------|----------|----------|
| customer | ??隞撌?| ??隞撌?| ??| ??|
| company_user | ??撘??+ ?? | ??撘??+ ?? | ??| ??|
| reviewer | ?????| ?????| ??|  |
| admin | ?????| ?????| ??| ??|

**摰?孵?**:
```python
# ?”餈誘嚗?閫嚗?if g.user.role == 'customer':
    projects = Project.query.filter_by(customer_user_id=g.user.id).all()
elif g.user.role == 'company_user':
    projects = [
        project for project in visible_projects
        if project.status == 'open_for_bids' or _company_has_project_access(project, company.id)
    ]

# 霂行?霈輸
_require_project_access(project)
```

---

### 4.4 鈭悅?唳?氖

| 閫 | ?”霈輸 | ?絲?? | 閫??? |
|------|----------|----------|----------|
| customer | ???芸楛憿寧 | ??憿寧銝蜓 | ??|
| company_user | ??瘨??芸楛 | ????憿寧 | ??|
| reviewer | ?????| ??| ??|
| admin | ?????| ??| ??|

**摰?孵?**:
```python
# ?”餈誘
if g.user.role == 'customer':
    disputes = [item for item in disputes if item.project.customer_user_id == g.user.id]
elif g.user.role == 'company_user':
    disputes = [
        item for item in disputes
        if item.against_company_id == company_id or item.opened_by_user_id == g.user.id
    ]
```

---

## 鈭?蝡舀????
### 5.1 璅⊥銝剔???璉??
#### ?砍?” - 瘛餃???曄內?批
```html
{% if g.user and (g.user.role == 'admin' or g.user.role == 'reviewer' or (g.user.role == 'company_user' and not g.user.company_id)) %}
<div class="row mb-3">
    <div class="col-md-6">
        <a href="{{ url_for('companies.add_company') }}" class="btn btn-primary">
            <i class="fas fa-plus"></i> Add Company
        </a>
    </div>
</div>
{% endif %}
```

**?餉?**:
- admin ??reviewer: ?餅?曄內
- company_user: 隞?芸??豢?曄內
- customer: 銝蝷?
---

#### ?砍霂行? - ????曄內?批
```html
<!-- 蝻?? -->
{% if g.user and (g.user.role == 'admin' or g.user.role == 'reviewer' or can_manage_company(g.user, company)) %}
<a href="{{ url_for('companies.edit_company', id=company.id) }}" class="btn btn-warning">Edit</a>
{% endif %}

<!-- 霈∠?霂?? -->
{% if g.user and (g.user.role == 'admin' or g.user.role == 'reviewer' or can_manage_company(g.user, company)) %}
<form action="{{ url_for('companies.calculate_score', id=company.id) }}" method="POST">
    <button type="submit" class="btn btn-success">Calculate Score</button>
</form>
{% endif %}
```

---

#### 韐瑟狡?” - ????曄內?批
```html
<!-- 摰⊥? -->
{% if g.user and g.user.role in ['admin', 'reviewer'] %}
<a href="{{ url_for('loans.review_application', id=application.id) }}" class="btn btn-primary">Review</a>
{% endif %}

<!-- 餈狡? -->
{% if g.user and (g.user.role == 'admin' or g.user.role == 'reviewer' or (g.user.role == 'company_user' and application.company_id == g.user.company_id)) %}
<button type="submit" class="btn btn-success">Repay</button>
{% endif %}
```

---

#### 憿寧?” - ????曄內?批
```html
<!-- ??? -->
{% if g.user and g.user.role == 'company_user' and project.status == 'open_for_bids' and can_bid %}
<button type="submit" class="btn btn-primary">Submit Bid</button>
{% endif %}

<!-- ?亙???? -->
{% if g.user and (g.user.role == 'admin' or (g.user.role == 'customer' and project.customer_user_id == g.user.id)) %}
<form action="{{ url_for('projects.accept_project_bid', project_id=project.id, bid_id=bid.id) }}" method="POST">
    <button type="submit" class="btn btn-success">Accept Bid</button>
</form>
{% endif %}
```

---

### 5.2 撖潸?????批

```html
<!-- 撖潸??-->
<ul class="nav-menu">
    <li><a href="{{ url_for('main.index') }}">?? Home</a></li>
    <li><a href="{{ url_for('main.dashboard') }}">?? Dashboard</a></li>
    <li><a href="{{ url_for('companies.list_companies') }}">? Companies</a></li>
    
    <!-- 隞?貊?瑯恣???恣?詨??航? -->
    {% if g.user and g.user.role in ['company_user', 'admin', 'reviewer'] %}
    <li><a href="{{ url_for('loans.list_loans') }}">? Loan Applications</a></li>
    {% endif %}
    
    <li><a href="{{ url_for('projects.list_projects') }}">?妤 Projects</a></li>
    <li><a href="{{ url_for('disputes.list_disputes') }}">?? Disputes</a></li>
    
    <!-- 隞恣???恣?詨??航? -->
    {% if g.user and g.user.role in ['admin', 'reviewer'] %}
    <li><a href="{{ url_for('companies.compare_reports') }}">?? Compare Reports</a></li>
    <li><a href="{{ url_for('admin.audit_logs') }}">?屁 Audit Logs</a></li>
    {% endif %}
</ul>
```

---

## ?准恣霈⊥敹扇敶?
### 6.1 霈啣???雿掩??
| ??蝐餃? | 閫血?閫 | ?格?蝐餃? | 霂湔? |
|---------|---------|---------|------|
| user_registered | 蝟餌? | User | ?冽瘜典? |
| user_logged_in | ?冽 | User | ?冽?餃? |
| user_logged_out | ?冽 | User | ?冽?餃 |
| company_created | admin/reviewer/company_user | Company | ?遣?砍 |
| company_updated | admin/reviewer/company_user | Company | ?湔?砍 |
| company_deleted | admin | Company | ??砍 |
| trust_score_calculated | admin/reviewer/company_user/customer | Company | 霈∠?霂? |
| loan_application_created | company_user | LoanApplication | ?唾窈韐瑟狡 |
| loan_application_approved | admin/reviewer | LoanApplication | ?孵?韐瑟狡 |
| loan_application_rejected | admin/reviewer | LoanApplication | ??韐瑟狡 |
| loan_disbursed | admin/reviewer | LoanApplication | ?韐瑟狡 |
| loan_repayment_recorded | company_user/admin/reviewer | LoanApplication | 餈狡霈啣? |
| project_created | customer/admin | Project | ?遣憿寧 |
| project_updated | customer/admin | Project | ?湔憿寧 |
| bid_submitted | company_user/admin | ProjectBid | ?漱?? |
| bid_accepted | customer/admin | ProjectBid | ?亙??? |
| milestone_submitted | company_user/admin | ProjectMilestone | ?漱??蝣?|
| milestone_approved | customer/admin | ProjectMilestone | ?孵???蝣?|
| dispute_opened | customer/company_user/admin | DisputeCase | ?絲鈭悅 |
| dispute_resolved | admin/reviewer | DisputeCase | 閫?鈭悅 |

---

### 6.2 摰∟恣?亙?蝏?

```python
class AuditLog(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    action = db.Column(db.String(100))  # ??蝐餃?
    target_type = db.Column(db.String(50))  # ?格?蝐餃?
    target_id = db.Column(db.Integer)  # ?格? ID
    actor_user_id = db.Column(db.Integer, db.ForeignKey('user.id'))  # ??鈭?    actor = db.relationship('User', foreign_keys=[actor_user_id])
    details_json = db.Column(db.Text)  # 霂衣?靽⊥嚗SON嚗?    created_at = db.Column(db.DateTime, default=datetime.now(timezone.utc))
```

---

## 銝???蝵格?雿喳?頝?
### 7.1 ??霈曇恣??

1. **?撠?????*: ?冽隞???極雿????撠???   - ?砍?冽嚗?質挪?株撌勗?貊??唳
   - 摰Ｘ嚗?賜恣?撌梁?憿寧
   - 摰⊥???芾摰⊥嚗??賢???
2. **?提?氖??**: ?喲???閬?鈭箏???   - 韐瑟狡?唾窈嚗?貊?瘀?vs 韐瑟狡摰⊥嚗恣?詨?/蝞∠???
   - 憿寧?遣嚗恥?瘀?vs 憿寧??嚗?貊?瘀?
   - ??蝣?鈭歹??砍?冽嚗s ??蝣恣?對?摰Ｘ嚗?
3. **?唳?氖??**: ???唳???脤?蝳?   - ?砍?唳嚗?貊?瑚??撌?   - 韐瑟狡?唳嚗?貊?瑚??撌?   - 憿寧?唳嚗????喟頂餈誘

4. **摰∟恣餈質葵??**: ???格?雿?敹?   - ?遣??啜??斗?雿?   - 摰⊥??蝏?雿?   - 韏??詨??

---

### 7.2 摰撱箄悅

1. **摰?摰⊥擃??揭??*
   - reviewer ??admin 閫??颲之
   - 撱箄悅摰?摰⊥???亙?
   - ? admin 韐行?圈?

2. **?撘虜??**
   - 憸????
   - ?極雿?渡?摰⊥??
   - 憭折?韐瑟狡摰⊥

3. **韐行 inactive 憭?**
   - ?蝳蝳餉??極韐行
   - 摰?皜??芣?瘣餉揭??
4. **??蝡臬???霂?*
   - ?垢嚗??格蝷箸?塚??冽雿?嚗?   - ?垢嚗???霂?摰靽?嚗?   - 銝?韏?銝撉???
5. **????鈭活蝖株恕**
   - ???嚗?蝡舐＆霈文笆霂?
   - 韐瑟狡摰⊥嚗?蝡舐????   - 韏???嚗恣霈⊥敹扇敶?
---

### 7.3 撣貉??桅??

**?桅? 1: ?砍?冽???啣?詨?銵?*
- 璉?伐?`user.company_id` ?臬銝?None
- 閫?嚗＆靽?瑕歇?唾??砍

**?桅? 2: ?砍?冽??蝻??砍**
- 璉?伐?`user.company_id == company.id`
- 閫?嚗＆霈斤?瑕?甇?＆???
**?桅? 3: ?砍?冽?漱韐瑟狡?唾窈憭梯揖**
- 璉?伐??砍?臬?唾??啁??- 璉?伐?憿寧?臬?唾??啣?賂?憒?嚗?- 閫?嚗＆靽?詨?憿寧?喟頂甇?＆

**?桅? 4: 摰Ｘ???憿寧**
- 璉?伐?`project.customer_user_id == user.id`
- 閫?嚗＆霈日★?格???

**?桅? 5: ?砍?冽????**
- 璉?伐??砍撉??嗆?  - ?撉?嚗licence_verification_status == 'verified'`
  - 靽撉?嚗insurance_verification_status == 'verified'`
  - 摰?寡悌嚗safety_training_coverage >= 80`
- 閫?嚗???賊?霂?
---

## ?怒餌?

### 8.1 閫??璁?

| ????| customer | company_user | reviewer | admin |
|-------|----------|--------------|----------|-------|
| **?砍蝞∠?** | ??| ??(1 銝迎??芣?) | ??(??? | ??(??? |
| **韐瑟狡?唾窈** | ??| ??(?芣?) | ??| ??|
| **韐瑟狡摰⊥** | ??| ??| ??| ??|
| **憿寧蝞∠?** | ??(?芣?) | ??(??) | ??(??? | ??(??? |
| **鈭悅憭?** | ??(?絲) | ??(?絲) | ??(閫?) | ??(閫?) |
| **摰∟恣?亙?** | ??| ??| ??| ??|
| **???** | ??| ??| ??| ??|

---

### 8.2 ?喲霈曇恣鈭桃

1. **?菜暑??擖啣蝟餌?**: ?舐??蝙??`@login_required` ??`@role_required`
2. **蝏?摨西挪?格??*: ?箔?銝?餉?????霂?3. **摰摰∟恣餈質葵**: ???雿餈賣滲
4. **憭祗閮?舀?**: ???內?舀?銝剛??5. **??蝡臬????*: 璅⊥?曄內?批 + ?垢??撉?

---

### 8.3 ?拙?撱箄悅

1. **憓?閫**: 
   - `auditor` (隞?恣霈⊥敹?
   - `loan_officer` (隞??晰甈?
   - `project_manager` (??憿寧)

2. **???漣**:
   - admin ?蛹 `super_admin` ??`operator`
   - reviewer ?蛹 `senior_reviewer` ??`junior_reviewer`

3. **?唳??**:
   - 憓??券/?箏?蝥批??桅?蝳?   - 憓?韐瑟狡憸漲?漣摰⊥

4. **??摰⊥??*:
   - 憭折?韐瑟狡?閬?蝥批恣??   - ??憿寧?閬?鈭箇＆霈?
---

**?﹝???園**: 2026-03-24  
**蝟餌??**: renovation-credit-system v2.0  
**????*: ?砍?”?唳?氖靚嚗ompany_user 隞??芸楛?砍嚗?
